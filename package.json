{
  "name": "atlas-go1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:memory": "node tools/build/generate_memory_assets.mjs",
    "check:memory": "node tools/build/generate_memory_assets.mjs --check",
    "test": "node --test tests/*.test.js && python3 tools/qa/validate_lessons.py && npm run check:memory"
  }
}
