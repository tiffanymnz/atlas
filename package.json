{
  "name": "atlas-go1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:memory": "node tools/build/generate_memory_assets.mjs",
    "check:memory": "node tools/build/generate_memory_assets.mjs --check",
    "check:knowledge": "node tools/qa/validate_knowledge_graph.mjs",
    "audit:release": "node tools/qa/release_audit.mjs",
    "test": "node --test tests/*.test.js && python3 tools/qa/validate_lessons.py && npm run check:knowledge && npm run check:memory && npm run audit:release"
  }
}
